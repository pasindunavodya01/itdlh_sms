# Example environment variables for the server
# Path to chatbot JSON data file. Can be absolute or relative to the project root.
# If omitted, the server will use server/chatbot_data.json by default.
CHATBOT_DATA_FILE=chatbot_data.json

# DB connection example
# DB_HOST=127.0.0.1
# DB_USER=root
# DB_PASSWORD=secret
# DB_NAME=itdlh_sms

# Firebase admin service account options (pick one - do NOT commit the JSON file)
# Option A: path to a JSON file containing the service account
# Example: FIREBASE_SERVICE_ACCOUNT_PATH=server/firebase/serviceAccountKey.json
# Option B: raw JSON (not recommended in shells) - works for some hosts
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account", ...}
# Option C (recommended for CI/hosting): base64-encoded JSON
# Set FIREBASE_SERVICE_ACCOUNT_B64 to the base64 value of the JSON file.
# Example (PowerShell):
#   $b = [Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes((Get-Content -Raw -Path 'C:\path\to\serviceAccountKey.json')))
#   # then set FIREBASE_SERVICE_ACCOUNT_B64 in your host to the value of $b
# Option D: Use Google Application Default credentials
#   Set GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json in your environment
